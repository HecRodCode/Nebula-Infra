services:
  app:
    image: jc21/nginx-proxy-manager:latest
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "81:81"
    environment:
      TZ: "America/Bogota"
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt